\documentclass[12pt]{article}
% Page setup
\usepackage[margin=1in]{geometry}
\usepackage{parskip}
\usepackage{titling}
\setlength{\droptitle}{-6em} 

% Math packages
\usepackage{amsmath, amsthm, amssymb, amsfonts}
\usepackage{mathtools}
\makeatletter
\g@addto@macro\th@plain{\normalfont} % remove italics from theorems (plain style)
\makeatother

\usepackage{hyperref}
\hypersetup{hidelinks}

% Commutative diagrams and proofs
\usepackage{tikz-cd}
\usepackage{proof}
\usepackage{stmaryrd}

% Unordered lists
\usepackage{enumitem}
\newlist{related}{itemize}{1}
\setlist[related]{label=\textbullet}
\newlist{distinct}{itemize}{1}
\setlist[distinct]{label=--}


% Theorem environments
\newtheorem{theorem}{Theorem}[subsection]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{fact}[theorem]{Fact}
\newtheorem{remark}[theorem]{Remark}
\newtheorem*{definition*}{Definition}
\newtheorem*{remark*}{Remark}
\newtheorem*{example*}{Example}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}

% Useful commands for category theory
\newcommand{\alp}{\mathbf{Alph}}
\newcommand{\mc}{\mathcal}
\newcommand{\ini}{\operatorname{ini}}
\newcommand{\ter}{\operatorname{ter}}
\newcommand{\cod}{\operatorname{cod}}
\newcommand{\dom}{\operatorname{dom}}

\newcommand{\List}{\operatorname{List}}
\newcommand{\lop}{\operatorname{[ \ ]}}
\newcommand{\len}{\operatorname{len}}
\newcommand{\type}{\operatorname{type}}

\newcommand{\Exc}{\operatorname{Exception}}

\newcommand{\Kl}{\operatorname{Kl}}
\newcommand{\plus}{\operatorname{+}}

\newcommand{\Cat}{\mathcal{C}at}
\newcommand{\Vect}{\mathrm{Vect}_\mathbf{k}}
\newcommand{\Hom}{\operatorname{Hom}}
\newcommand{\Nat}{\operatorname{Nat}}
\newcommand{\id}{\mathrm{id}}
\newcommand{\op}{\mathrm{op}}
\newcommand{\obj}{\operatorname{obj}}
\newcommand{\arr}{\operatorname{arr}}
\newcommand{\adj}{\operatorname{adj}}
\newcommand{\Par}{\operatorname{par}}

\newcommand{\coadj}{\operatorname{coadj}}
\newcommand{\colim}{\operatorname*{colim}}

% Arrows
\newcommand{\ra}{\rightarrow}
\newcommand{\from}{\leftarrow}
\newcommand{\To}{\Rightarrow}
\newcommand{\xto}{\xrightarrow}
\newcommand{\xfrom}{\xleftarrow}

\renewcommand{\labelitemi}{--} % Level 1: Dashes
\renewcommand{\labelitemii}{$\circ$} % Level 2: Circles
\renewcommand{\labelitemiii}{$\bullet$} % Level 3: Bullets (default)


\title{Notes: Category Theory in Context}
\author{Alyson Mei}
\date{\today}


\begin{document}

\maketitle

These notes are intended as a concise overview of the book \href{https://emilyriehl.github.io/files/context.pdf}{Category Theory in Context} by Emiliy Riehl.  

\tableofcontents

\section{Categories, functors, natural transformations}

\subsection{Abstract and concrete categories}

\begin{definition}[Category] Notation: $\mc{C}, \mc{D}, \mc{E}, \ldots$
\end{definition}

\begin{remark}[Correspondence between objects and identity morphisms]
\end{remark}

\begin{example}[Concrete categories]
    $\mc{S}et$-based categories:
    \begin{itemize}
        \item $\mc{S}et$, $\mc{T}op$;
        \item $\mc{S}et_*$, $\mc{T}op_*$ (pointed);
        \item $\mc{G}rp$, $\mc{R}ing$, $\mc{F}ield$;
        \item $\mc{A}b\mc{G}rp$, $\mc{V}ect_{\mathbf{k}}$, $\mc{M}od_R$;
        \item $\mc{G}raph$, $\mc{D}ir\mc{G}raph$;
        \item $\mc{M}an$, $\mc{M}eas$ -- manifolds and measurable spaces;
        \item $\mc{P}oset$;
        \item $\mc{C}h_R$ -- chain complexes;
        \item $\mc{M}odel_\mathbb{T}$ -- $\sigma$-structures that model $\mathbb{T}$ (see universal algebra).
    \end{itemize}
\end{example}

\begin{remark*} 
    The category $\mc{S}et$ implicitly depends on the underlying set theory.
\end{remark*}

\begin{example}[Abstract categories]
    \hfill
    \begin{itemize}
        \item $\mc{M}at_R$ -- objects are integers, arrows $m \to n$ are $m \times n$ matrices with values in $R$;
        \item $\mathcal{B}G$;
        \item $(P, \leq)$ and ordinal $\alpha = \lbrace \beta \mid \beta < \alpha \rbrace$ as categories;
        \item $\mc{H}tpy$, $\mc{H}tpy_*$, $\mc{M}easure$.
    \end{itemize}
\end{example}

\begin{remark}[Russel's paradox]
\end{remark}

\begin{definition}[Small category]
    A category with only a set's worth of arrows.
\end{definition}

\begin{definition}[Locally small category] 
    A category with only set's worth of arrows between any pair of objects.
\end{definition}

\begin{definition}[Isomorphism, automorphism, endomorphism]
\end{definition}

\begin{example}[Isomorphisms in categories]
\end{example}

\begin{definition}[Groupoid] 
    A groupoid is a category in which every morphism is an isomorphism.
\end{definition}

\begin{example}[Examples of groupoids]
\end{example}

\begin{definition*}[Subcategory]
\end{definition*}

\begin{lemma}[Maximal groupoid] 
    Any category $\mc{C}$ contains a maximal groupoid, the subcategory generated by isomorphism of $\mc{C}$.
\end{lemma}

\begin{definition*}[Slice categories $c/\mc{C}$ and $\mc{C}/c$]    
\end{definition*}

\subsection{Duality}
\begin{definition}[Opposite category $\mc{C}^\op$]
\end{definition}

\begin{remark*}
    This duality is purely syntactic. Its usefulness becomes apparent when
    a) considering opposite constructions, b) working with functors.
\end{remark*}

\begin{example}[Opposite categories] 
    $\mc{M}at_R^\op$, $(P, \leq)^\op$, $(\mc{B}G)^\op$.
\end{example}

\begin{lemma}
    The following are equivalent (in a locally small $\mc{C}$):
    \begin{enumerate}
        \item $f: x \to y$ is an iso in $\mc{C}$.
        \item For all $c: \obj(\mc{C})$, post-composition with $f$ defines a bijection
        \[
            f_*: \mc{C}(c, x) \to \mc{C}(c, y).
        \]
        \item For all $c: \obj(\mc{C})$, pre-composition with $f$ defines a bijection
        \[
            f_*: \mc{C}(y, c) \to \mc{C}(x, c).
        \]
    \end{enumerate}
\end{lemma}

\begin{remark}[Note on representability]
\end{remark}

\addtocounter{theorem}{2}
\begin{definition}[Monomorphism, epimorphism]
    % https://q.uiver.app/#q=WzAsNCxbMSwwLCJ4Il0sWzIsMCwieSJdLFswLDAsInoiXSxbMywwLCJ3Il0sWzAsMSwiZiIsMCx7Im9mZnNldCI6LTF9XSxbMCwxLCJnIiwyLHsib2Zmc2V0IjoxfV0sWzIsMCwiXFx0ZXh0e2VwaX0iXSxbMSwzLCJcXHRleHR7bW9ub30iXV0=
    \[\begin{tikzcd}
        z & x & y & w
        \arrow["{\text{epi}}", from=1-1, to=1-2]
        \arrow["f", shift left, from=1-2, to=1-3]
        \arrow["g"', shift right, from=1-2, to=1-3]
        \arrow["{\text{mono}}", from=1-3, to=1-4]
    \end{tikzcd}\text{.}\]
    (to remember: epi $\to$ mono)
\end{definition}

\begin{example}[Mono in $\mc{S}et$ are precisely the injections]
\end{example}

\begin{example}[Section (right inverse), retraction (left inverse); split mono / epi]
\end{example}

\begin{example}[Iso $\Rightarrow$ mono and epi; counterexample $\mathbb{Z} \hookrightarrow \mathbb{Q}$ in $\mc{R}ing$ for the converse]
\end{example}

\begin{lemma}[Compositions of mono / epi]
    \hfill
    \begin{enumerate}
        \item Composable $f$ and $g$ are mono $\Rightarrow$ $gf$ is mono. Dually, $f$ and $g$ are epi $\Rightarrow$ $gf$ is epi.
        \item $gf$ is mono $\Rightarrow$ $f$ is mono. Dually, $gf$ is epi $\Rightarrow$ $g$ is epi.
    \end{enumerate}
\end{lemma}

\subsection{Functoriality}

\begin{definition}[Functor] Notation: $F: \mc{C} \to \mc{D}$.
\end{definition}

\begin{example}[Functors]
    \hfill
    \begin{itemize}
        \item Power set functor $P: \mc{S}et \to \mc{S}et$;
        \item Forgetful functors $U: \mc{C} \to \mc{S}et$, itermediate forgetful functors (e.g. $\mc{M}od_R \to \mc{A}b$, $\mc{G}r \to \mc{S}et_*$);
        \item Homotopy class functors $\mc{T}op_{(*)} \to \mc{H}tpy_{(*)}$;
        \item Fundamental group $\pi_1: \mc{T}op_* \to \mc{G}rp$ and its factor $\pi_1: \mc{H}tpy_* \to \mc{G}rp$ through $\mc{T}op_* \to \mc{H}tpy_*$, fundamental groupoid $\Pi_1: \mc{T}op \to \mc{G}roupoid$;
        \item $n$-cycles $Z_n$, $n$-boundary $B_n$ and $n$-th homology $H_n$ typed $\mc{C}h_R \to \mc{M}od_R$, all degrees simultaneously give $Z_*, B_*, H_*: \mc{C}h_R \to \mc{G}r\mc{M}od_R$ (codomain is the category of graded $R$-modules);
        \item Free functors $F: \mc{S}et \to \mc{C}$ (left adjoints to forgetful functors);
        \item Derivative $D: \mc{E}uclid_* \to \mc{M}at_\mathbb{R}$;
        \item $M^-: \mc{F}in_* \to \mc{S}et$ for a commutative monoid $M$.
    \end{itemize}
\end{example}

\begin{theorem}[Brouwer Fixed Point Theorem] Any continuous endomorphism of a 2-dimensional disk $D^2$ has a fixed point.
\end{theorem}

\begin{example}[Example for topological data analysis]
\end{example}

\begin{definition}[Contravariant functor] $F: \mc{C}^\op \to \mc{D}$.
\end{definition}

\addtocounter{theorem}{1}

\begin{example}[Contravariant functors]
    \hfill
    \begin{itemize}
        \item Contravariant power set $P: \mc{S}et^\op \to \mc{S}et$;
        \item Dual vector space $*: \mc{V}ect_\mathbf{k}^\op \to \mc{V}ect_\mathbf{k}$;
        \item Continuity $O: \mc{T}op^\op \to \mc{P}oset$;
        \item Zarisky topology $\mc{CR}ing \to \mc{T}op$;
        \item Presheafs $\mc{C}^\op \to \mc{S}et$ and simplex category.
    \end{itemize}
\end{example}

\begin{lemma}[Functors preserve isomorphisms]
\end{lemma}

\begin{example}[Functors $\mc{B}G \to \mc{C}$ ($\mc{B}G^\op \to \mc{C}$) are left (right) group actions]
\end{example}

\begin{corollary}[Functorial action of a group]
\end{corollary}

\begin{definition}[Partial hom functors]
    $\mc{C}(c, -): \mc{C} \to \mc{S}et$, $\mc{C}(-, c): \mc{C}^\op \to \mc{S}et$ for a locally small $\mc{C}$.
\end{definition}

\begin{definition}[Product of categories]
    Notation: $\mc{C} \times \mc{D}$.
\end{definition}

\begin{definition}[Hom functor]
    $\mc{C}(-, -): \mc{C}^\op \times \mc{C} \to \mc{S}et.$
\end{definition}

\begin{definition*}[Categories of categories]
    \hfill
    \begin{itemize}
        \item $\mc{C}at$ -- category of small categories;
        \item $\mc{CAT}$ -- category of locally small categories.
    \end{itemize}
\end{definition*}

\begin{example}[Isomorphisms of categories]
    \hfill
    \begin{itemize}
        \item $(-)^\op: \mc{CAT} \to \mc{CAT}$;
        \item $(-)^{-1}: \mc{B}G \to \mc{B}G^\op$;
        \item Groupoid is isomorphic to its opposite;
        \item $\mc{M}od_R \simeq {}_{R^\op} \mc{M}od$;
        \item $\mc{M}at_R \simeq \mc{M}at_R^\op$.
    \end{itemize}
\end{example}

\begin{example}[Galois extension]
\end{example}

\subsection{Naturality}

\begin{definition}[Natural transformation, natural isomorphism]
    Notation: $\alpha: F \to G$.
\end{definition}

\addtocounter{theorem}{1}
\begin{example}[Natural transformations]
    \hfill
    \begin{itemize}
        \item $\text{ev}: \id_{\mc{V}ect} \to **$, iso on $\mc{F}d\mc{V}ect$;
        \item $G$-equivariant map $\mc{B}G \to \mc{C}$;
        \item Open and closed subset functors $O, C: \mc{T}op^\op \to \mc{S}et$ are naturally isomorphic;
    \end{itemize}
\end{example}

\begin{proposition}[Example of an iso that is not natural (torsion subgroup)]
\end{proposition}

\begin{example}[Riesz representation theorem]
\end{example}

\begin{example}[Natural transformation between hom functors]
\end{example}

\addtocounter{theorem}{1}
\begin{example}[Categorification of natural numbers]
\end{example}

\subsection{Equivalence of categories}

\begin{lemma}[Internal representation of natural transformations]
    Fixing a parallel pair of functors $F, G:\mc{C} \to \mc{D}$, natural transformations $\alpha: F \to G$ correspond bijectively to functors $H:  \mc{C} \times 2 \to \mc{D}$ such that
    % https://q.uiver.app/#q=WzAsNCxbMSwwLCJcXG1hdGhjYWx7Q30gXFx0aW1lcyAyIl0sWzAsMCwiXFxtYXRoY2Fse0N9Il0sWzIsMCwiXFxtYXRoY2Fse0N9Il0sWzEsMSwiXFxtYXRoY2Fse0R9Il0sWzEsMCwiaV8wIl0sWzIsMCwiaV8xIiwyXSxbMCwzLCJIIl0sWzEsMywiRiIsMl0sWzIsMywiRyJdXQ==
    \[\begin{tikzcd}
        {\mathcal{C}} & {\mathcal{C} \times 2} & {\mathcal{C}} \\
        & {\mathcal{D}}
        \arrow["{i_0}", from=1-1, to=1-2]
        \arrow["F"', from=1-1, to=2-2]
        \arrow["H", from=1-2, to=2-2]
        \arrow["{i_1}"', from=1-3, to=1-2]
        \arrow["G", from=1-3, to=2-2]
    \end{tikzcd}.\]
\end{lemma}

\addtocounter{theorem}{2}
\begin{definition}[Equivalence of categories] Notation: $\mc{C} \sim \mc{D}.$
\end{definition}

\begin{lemma}[Equivalence of categories defines an equivalence relation]
\end{lemma}

\begin{example}[Equivalence between the category of pointed sets and the category of sets with partial functions]
\end{example}

\begin{definition}[Full, faithful, essentially surjective on objects functors]
\end{definition}

\begin{remark}[(Full) embedding, full subcategory]
\end{remark}

\begin{theorem}[Characterizing equivalence of categories]
    A functor defininig an equivalence of categories is full, faithful and essentially surjective on objects. Assumming the axiom of choice, any functor with these properties defines an equivalence of categories.
\end{theorem}

\addtocounter{theorem}{1}

\begin{corollary}[An equivalence between abstract and concrete linear algebra]
\end{corollary}

\begin{proposition}
    Any connected groupoid is equivalent to the automorphism group of any of its objects.
\end{proposition}

\begin{corollary}
    In a path-connected space $X$, any choice of basepoint $x \in X$ yields an isomorphic fundamental group $\pi_1(X, x)$.
\end{corollary}

\begin{remark}
    Frequently, one functor of an equivalence of categories can be defined canonically, while the inverse requires the axiom of choice.
\end{remark}

\begin{definition}[Skeleton and skeletal category] Notation: $\text{sk}\mc{C}$.
\end{definition}

\begin{remark}[Construction of skeleton]
\end{remark}

\begin{example}[Skeletons]
\end{example}

\begin{example}[A categorification of the orbit-stabilizer theorem]
\end{example}

\subsection{The art of the diagram chase}

\addtocounter{theorem}{1}

\begin{definition}[Monoid object]
    \hfill
    \begin{itemize}
        \item Monoid in $\mc{S}et$;
        \item Topological monoid;
        \item Unital ring;
        \item $\mathbf{k}$-algebra.
    \end{itemize}
\end{definition}

\addtocounter{theorem}{1}

\begin{lemma}[Functors preserve commutative diagrams]
\end{lemma}

\addtocounter{theorem}{6}

\begin{lemma}[Rewriting in compositions of morphisms]
    \[ 
        f_j \ldots f_k = g_m \ldots g_1   \Rightarrow f_1 \ldots f_j \ldots f_k \ldots f_n = f_1 \ldots g_m \ldots g_1 \ldots f_n.
    \]
\end{lemma}

\begin{lemma}[Reversing iso on the diagram]
\end{lemma}

\begin{lemma}[Reversing multimple isos on the diagram]
\end{lemma}

\begin{definition}[Initial and terminal objects]
\end{definition}

\begin{example}[Examples of initial and terminal objects]
    \hfill
    \begin{itemize}
        \item Empty set / one-element set in $\mc{S}et$;
        \item Empty and singleton space in $\mc{T}op$;
        \item Every one-element set in $\mc{S}et_*$;
        \item Zero module is both initial and terminal in $\mc{M}od_R$, the trivial group is both initial and terminal in $\mc{G}r$;
        \item $\mathbb{Z}$ is the initial object in $\mc{R}ing$;
        \item $\mc{F}ield$ has neigher initial nor terminal objects;
        \item The empty category is initial and the category $1$ is terminal in $\mc{C}at$;
        \item A global minimal and a global maximal elements (if they exist) are the initial and terminal ojbects in a preorder.
    \end{itemize}
\end{example}

\begin{lemma}[Compositions from initial / to terminal objects]
\end{lemma}

\begin{definition}[Concrete category]
    A concrete category is a category $\mc{C}$ equipped with a faithful functor $U: \mc{C} \to \mc{S}et$.
\end{definition}

\begin{lemma}[Faithful preimage of a commutative diagram]
    $U: \mc{C} \to \mc{D}$ is faithful $\Rightarrow$ preimage of commutative diagram in $\mc{D}$ is commutative in $\mc{C}$.
\end{lemma}

\addtocounter{theorem}{1}

\begin{remark}[On cancellability]
    Adjacent squares commute $\Rightarrow$ external square commutes. Converse need not hold.
\end{remark}

\begin{lemma}
    Consider morphisms with the indicated sources and targets
    % https://q.uiver.app/#q=WzAsNixbMCwwLCJhIl0sWzEsMCwiYiJdLFsxLDEsImInIl0sWzAsMSwiYSciXSxbMiwwLCJjIl0sWzIsMSwiYyciXSxbMCwzLCJnIiwyXSxbMSwyLCJoIiwyXSxbNCw1LCJ0IiwyXSxbMCwxLCJmIl0sWzEsNCwiaiJdLFszLDIsImsiLDJdLFsyLDUsIm0iLDJdXQ==
    \[\begin{tikzcd}
        a & b & c \\
        {a'} & {b'} & {c'}
        \arrow["f", from=1-1, to=1-2]
        \arrow["g"', from=1-1, to=2-1]
        \arrow["j", from=1-2, to=1-3]
        \arrow["h"', from=1-2, to=2-2]
        \arrow["t"', from=1-3, to=2-3]
        \arrow["k"', from=2-1, to=2-2]
        \arrow["m"', from=2-2, to=2-3]
    \end{tikzcd}\]
    and suppose that the outer rectangle commutes. This data defines a commutative rectangle if either:
    \begin{enumerate}
        \item[$(i)$] the right-hand square commutes and $m$ is a mono;
        \item[$(ii)$] the left-hand square commutes and $f$ is an epi.  
    \end{enumerate}
\end{lemma}

\subsection{The 2-category of categories}

\end{document}
